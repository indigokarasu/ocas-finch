__pycache__/
*.pyc
.DS_Store

# Deprecated/trash — never publish to GitHub (untracked via auto-sync git add -A)
archive/
.archive/

# Junk that must never publish to GitHub (untracked via auto-sync git add -A)
OPERATOR-FYI-memory_guard.md
TODO.md
config.json
archive/
.archive/
assets/readme/hero.svg
